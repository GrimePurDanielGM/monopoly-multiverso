packages:
  - "apps/*"
  - "packages/*"

# Autorización explícita de scripts de build (pnpm 11): solo esbuild.
# Sin aprobación interactiva y sin autorizar todos los paquetes.
allowBuilds:
  esbuild: true
